diff -urN a/parkpilot/core/session_manager.py b/parkpilot/core/session_manager.py
--- a/parkpilot/core/session_manager.py	2026-04-11 22:53:26.000000000 +0000
+++ b/parkpilot/core/session_manager.py	2026-04-12 23:24:58.288775578 +0000
@@ -31,6 +31,7 @@
     created_at_local: str
     last_updated_local: str
     last_mode: str = "CW"
+    last_band: str = "20M"
 
 # ============================================================
 # HELPERS
@@ -127,7 +128,8 @@
         status="active",
         created_at_local=created_at_local_x,
         last_updated_local=created_at_local_x,
-        last_mode="CW",  
+        last_mode="CW",
+        last_band="20M",
     )
     save_session(session_dx)
     return session_dx, "created"
@@ -168,3 +170,14 @@
     session_dx.last_mode = last_mode_x.upper().strip()
     save_session(session_dx)
     return session_dx
+
+
+def set_last_band(last_band_x: str) -> Optional[SessionDX]:
+    session_dx = get_current_session()
+
+    if session_dx is None:
+        return None
+
+    session_dx.last_band = last_band_x.upper().strip()
+    save_session(session_dx)
+    return session_dx
diff -urN a/parkpilot/ui/web/app.py b/parkpilot/ui/web/app.py
--- a/parkpilot/ui/web/app.py	2026-04-11 22:53:26.000000000 +0000
+++ b/parkpilot/ui/web/app.py	2026-04-12 23:24:58.289238947 +0000
@@ -10,7 +10,8 @@
     close_current_session,
     get_current_session,
     set_active_operator,
-    set_last_mode,  
+    set_last_band,
+    set_last_mode,
     start_or_resume_session,
 )
 
@@ -98,7 +99,9 @@
 
         call_x = request.form.get("call", "").upper().strip()
         mode_x = request.form.get("mode", "CW").upper().strip()
-        set_last_mode(mode_x) 
+        band_x = request.form.get("band", session_dx.last_band).upper().strip()
+        set_last_mode(mode_x)
+        set_last_band(band_x)
         rst_sent_x = request.form.get("rst_sent", "599").strip()
         rst_rcvd_x = request.form.get("rst_rcvd", "599").strip()
         notes_x = request.form.get("notes", "").strip()
@@ -119,6 +122,7 @@
             "session_date_local": session_dx.session_date_local,
             "park_id": session_dx.park_id,
             "operator": session_dx.active_operator,
+            "band": band_x,
             "call": call_x,
             "mode": mode_x,
             "rst_sent": rst_sent_x,
diff -urN a/parkpilot/ui/web/templates/index.html b/parkpilot/ui/web/templates/index.html
--- a/parkpilot/ui/web/templates/index.html	2026-04-11 22:53:26.000000000 +0000
+++ b/parkpilot/ui/web/templates/index.html	2026-04-12 23:24:58.289682557 +0000
@@ -130,6 +130,26 @@
       </select>
     </div>
 
+
+    <div class="row">
+      <label>Band</label>
+      <select name="band" id="band_select">
+        <option value="160M" {% if session_dx.last_band == "160M" %}selected{% endif %}>160M</option>
+        <option value="80M" {% if session_dx.last_band == "80M" %}selected{% endif %}>80M</option>
+        <option value="60M" {% if session_dx.last_band == "60M" %}selected{% endif %}>60M</option>
+        <option value="40M" {% if session_dx.last_band == "40M" %}selected{% endif %}>40M</option>
+        <option value="30M" {% if session_dx.last_band == "30M" %}selected{% endif %}>30M</option>
+        <option value="20M" {% if session_dx.last_band == "20M" %}selected{% endif %}>20M</option>
+        <option value="17M" {% if session_dx.last_band == "17M" %}selected{% endif %}>17M</option>
+        <option value="15M" {% if session_dx.last_band == "15M" %}selected{% endif %}>15M</option>
+        <option value="12M" {% if session_dx.last_band == "12M" %}selected{% endif %}>12M</option>
+        <option value="10M" {% if session_dx.last_band == "10M" %}selected{% endif %}>10M</option>
+        <option value="6M" {% if session_dx.last_band == "6M" %}selected{% endif %}>6M</option>
+        <option value="2M" {% if session_dx.last_band == "2M" %}selected{% endif %}>2M</option>
+        <option value="70CM" {% if session_dx.last_band == "70CM" %}selected{% endif %}>70CM</option>
+      </select>
+    </div>
+
     <div class="row">
       <label>RST Sent</label>
       <input type="text" name="rst_sent" id="rst_sent" value="599">
